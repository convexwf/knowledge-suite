# knowledge-suite Journal - 2023-10-20

## 16:48:09 fix: strip section anchors in preview and skip list-child deduplication

User reported two issues with document d47582aa clipped from clipper:

1. `<!-- section_id:s-de25b261b9 -->` HTML comments visible in preview panel
   but not in reader. Root cause: preview uses markdown-it with `html: false`,
   which escapes raw HTML instead of stripping it. Reader has its own
   line-by-line parser that correctly skips these comment lines (reader.ts:260).
   Fix: added `stripSectionAnchors()` in render.ts that removes lines matching
   `/^<!--\s*section_id:\S+\s*-->\n/gm` before passing markdown to markdown-it.

2. List items duplicated: appearing both in a list section and as standalone
   paragraph sections. Root cause: `extractSections()` uses `querySelectorAll`
   with deep descendant search across the whole DOM tree. When `<li>` contains
   child elements like `<p>` or `<blockquote>`, those descendant elements are
   also matched and processed as separate sections alongside their parent list.
   Fix: added `isInsideListItem()` check alongside the existing
   `isInsideMediaContainer()` check to skip any element whose ancestor is
   `<li>`.

Files changed:
- apps/knowledge-web-clipper/src/markdown-preview/render.ts: +stripSectionAnchors
- apps/knowledge-ingest-server/src/parser.ts: +isInsideListItem

```
fix: strip section anchors in preview and skip list-child deduplication

- strip <!-- section_id:... --> HTML comments before markdown preview rendering
- skip DOM elements inside <li> in extractSections to avoid duplication
```

## 17:13:48 refactor: reorganize Makefile with one-click setup and experimental AI section

The Makefile was a flat list of import and AI targets with no structure,
help target, or one-click bootstrap. As the project is being promoted for
external use, a clean entry point is essential.

Reorganized into clearly labeled sections with self-documenting targets:
- `make help` as default target prints all targets with descriptions
- `make setup` — one-click bootstrap: npm install + build all + prints next
  steps (start server, load extension, token and endpoint details)
- `make setup-ai` — extends setup with Ollama model verification for AI
- `make dev` and `make dev-ai` — local server with/without AI env vars
- `make docker-up` and `make docker-up-ai` — Docker with/without AI, with
  token/env overrides
- All import, smoke, e2e, and cleanup targets preserved and grouped
- AI targets clearly marked as experimental throughout
- README Quick Start updated to reference `make setup` as primary entry point

Variables at the top allow overrides: SERVER_HOST, SERVER_PORT, SERVER_TOKEN,
AI_MODEL, AI_OLLAMA_URL.

```
refactor: reorganize Makefile with one-click setup and experimental AI section

- add make help as default target with grouped target listing
- add make setup for one-click bootstrap (install + build + instructions)
- add make setup-ai extending setup with ollama model verification
- add make dev-ai and make docker-up-ai for local/Docker with AI enabled
- group targets into Setup / Build / Dev / Docker / AI / Import / Smoke / Cleanup
- mark all AI targets as experimental
- update README Quick Start to reference make setup
```

